export default {
  methods: {
    /**
     * @method afterInit
     * @desc Easy extend after init
     * @param {HTMLElement} element
     */
    afterInit(element, editor) {
      // For extends methods
    }
  }
};